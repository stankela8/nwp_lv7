
<div class="container mt-5">
    <h1 class="text-danger">Došlo je do greške</h1>
    <p><strong>Poruka:</strong> <%= message %></p>
    <% if (error && error.stack) { %>
        <pre><%= error.stack %></pre>
    <% } %>
    <a href="/projects" class="btn btn-primary mt-3">Nazad na projekte</a>
</div>
